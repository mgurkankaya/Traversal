@model EntityLayer.Concrete.Reservation
@{
    ViewData["Title"] = "NewReservation";
    Layout = "~/Views/Shared/UserLoyout.cshtml";
}

<h1>New Reservation</h1>
<form method="post">
    <div class="form-group">
        @Html.Label("Lokasyon")
        @Html.DropDownListFor(x => x.Destionation, (List<SelectListItem>)ViewBag.v, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.Label("Kişi Sayısı")
        <input type="text" asp-for="PersonCount" class="form-control" />
    </div>
    <div class="form-group">
        @Html.Label("Tarih")
        <input type="date" asp-for="ReservationDate" class="form-control" />
    </div>
    <div class="form-group">
        @Html.Label("Açıklama")
        <input type="text" asp-for="Description" class="form-control" rows="5" />
    </div>
   @*  <div class="form-group">
        @Html.Label("Durum")
        <input type="text" asp-for="Status" class="form-control"  />
    </div> *@
    <button class="btn-sm btn-outline-primary">Rezervasyon Oluştur</button>
</form>

